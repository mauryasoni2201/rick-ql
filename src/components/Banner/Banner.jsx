const Banner = ({ src }) => {
    return (
        <div className="hero-banner">
            <img src={src} width={'1584'} height={'672'} alt="banner-image" />
        </div>
    );
};
export default Banner;
